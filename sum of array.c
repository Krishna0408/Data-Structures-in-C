#include<stdio.h>
int main()
{
int a[10],i,arr[i],size,sum=0;
scanf("%d",&size);
{
for(i=0;i<size;i++);
scanf("%d",&arr[i]);
}
for(i=0;i<size;i+2);
sum=sum+arr[i];
printf("%d",sum);
return 0;
}
